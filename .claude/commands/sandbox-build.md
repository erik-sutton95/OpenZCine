---
description: Build and test without modifying any source files
allowed-tools: Bash(just:*)
---

# Sandbox Build

Run native checks in a read-only mode — build and test the project without modifying any source
files. Report the result.

!just native-check
