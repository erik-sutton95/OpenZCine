# Live-monitor browser prototype

This self-contained browser prototype records the visual direction that preceded the native SwiftUI
monitor. It is design reference material, not production code and not part of the landing-page
deployment.

Open `simulator.html` for the device frame or `index.html` for the monitor surface directly. The
included `feed.jpg` is the same synthetic/demo feed used by the iOS asset catalog.
