# Archived implementation plans

These dated plans preserve implementation context and design decisions. They are not live agent
instructions. Claude Code and Codex follow the current rules in `AGENTS.md`, `CLAUDE.md`, and the
repository's `just` verification recipes.

Model assignments, temporary worktree names, local evidence paths, and retired orchestration-tool
commands have been removed where they no longer help explain the resulting design.
