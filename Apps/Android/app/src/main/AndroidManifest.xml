<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PTP-IP camera sockets (local network only; there is no server backend). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera-AP join: WifiNetworkSpecifier requestNetwork + bindProcessToNetwork. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- NSD/Wi-Fi camera discovery: NEARBY_WIFI_DEVICES on 13+ (never used
        for location — the app only finds its own camera), location below. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <!-- Android 12 requires COARSE alongside FINE; approximate is enough for NSD. -->
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="32" />
    <!-- Rear camera, used only while the operator explicitly opens the local
      Connection-wizard scanner. OCR stays on device and no frame is stored. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The scanner is optional: pairing and all other app surfaces remain
      usable on devices without a camera. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- USB-C tethering is optional hardware: Android's per-device consent is
      requested only after a compatible PTP interface is physically attached. -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <!-- Shares only ready copies in cacheDir/share/ready. The progressive
          no-backup cache and the transient share/staging directory never have
          a provider path, so external apps cannot receive a growing .part. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.media-share"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/media_share_paths" />
        </provider>
        <!-- Sensor-based portrait + landscape (iOS allows Portrait + LandscapeRight;
          "user" is the closest platform posture: it follows the rotation lock).
          configChanges keeps the activity (camera session, live feed, glass
          pipeline) alive across rotation; the zone map recomputes in compose.
          ponytail: reverse-landscape also rotates here (Android convention);
          pinning to one landscape would need a manual OrientationEventListener. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="user"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
