package com.opencapture.openzcine.diagnostics

import androidx.core.content.FileProvider

/** Dedicated component identity for the diagnostics-only provider authority. */
class DiagnosticsFileProvider : FileProvider()
