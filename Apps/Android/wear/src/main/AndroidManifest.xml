<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- This artifact can install only on a Wear OS device. It has no camera,
      network, Bluetooth, or storage permissions because it is a phone-mediated
      remote, never a Nikon/PPT transport endpoint. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_openzcine_wear"
        android:label="@string/app_name"
        android:theme="@style/Theme.OpenZCineWear">
        <!-- This is deliberately not standalone: every camera operation stays
          on the foreground handheld relay, which owns the only Nikon session. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />
        <activity
            android:name=".WearMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
